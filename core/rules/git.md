# Git Practices

- Write concise, descriptive commit messages using `/conventional-commits`
- Never commit secrets, API keys, or credentials
- Don't commit generated files that should be in .gitignore
